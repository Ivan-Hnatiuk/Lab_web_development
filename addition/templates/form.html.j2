<!doctype html>
<html lang="uk">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Форма</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='form.css') }}">
    </head>
<body>
    <h1>Надіслати дані</h1>

    {% if errors %}
    <div class="card error-card">
        <h2>Виправте помилки</h2>
        <ul>
            {% for message in errors.values() %}
            <li>{{ message }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endif %}

    <form action="{{ url_for('form') }}" method="post" novalidate>
        <div>
            <label for="name">Ім'я</label>
            <input id="name" name="name" type="text" placeholder="Ваше ім'я" value="{{ name|e }}" required>
            {% if errors.name %}
            <div class="error-text">{{ errors.name }}</div>
            {% endif %}
        </div>
        <div>
            <label for="email">Email</label>
            <input id="email" name="email" type="email" placeholder="name@example.com" value="{{ email|e }}" required>
            {% if errors.email %}
            <div class="error-text">{{ errors.email }}</div>
            {% endif %}
        </div>
        <div>
            <label for="age">Вік</label>
            <input id="age" name="age" type="number" min="0" placeholder="18" value="{{ age|e }}" required>
            {% if errors.age %}
            <div class="error-text">{{ errors.age }}</div>
            {% endif %}
        </div>
        <div>
            <label for="message">Повідомлення</label>
            <textarea id="message" name="message" rows="4" placeholder="Ваше повідомлення..." required>{{ message|e }}</textarea>
            {% if errors.message %}
            <div class="error-text">{{ errors.message }}</div>
            {% endif %}
        </div>
        <button type="submit">Надіслати</button>
        <div class="muted">Метод: GET показує форму, POST обробляє введені дані та перенаправляє на сторінку результатів.</div>
    </form>
</body>
</html>


