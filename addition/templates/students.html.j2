{% extends "base.html.j2" %}
{% block content %}
<div class="card">
    <h2>Список студентів</h2>
    <p class="muted">Натисніть на ім'я, щоб переглянути індивідуальні оцінки.</p>
</div>
{% if students %}
<ul>
    {% for student in students %}
    <li>
        <a href="{{ url_for('student_detail', student_id=student.id) }}">{{ student.name }}</a>
    </li>
    {% endfor %}
</ul>
{% else %}
<p class="muted">Студенти не знайдені.</p>
{% endif %}
{% endblock %}

