{% extends "base.html.j2" %}
{% block content %}
<div class="card">
    <h2>Редагування та видалення оцінок</h2>
    <p class="muted">
        Оберіть потрібний запис для редагування або видалення.
    </p>
</div>

{% if grades %}
<table>
    <thead>
        <tr>
            <th>#</th>
            <th>Студент</th>
            <th>Дисципліна</th>
            <th>Семестр</th>
            <th>Бали</th>
            <th>Дії</th>
        </tr>
    </thead>
    <tbody>
    {% for grade in grades %}
        <tr>
            <td>{{ loop.index }}</td>
            <td>{{ grade.student_name }}</td>
            <td>{{ grade.course_title }}</td>
            <td>{{ grade.semester }}</td>
            <td>{{ grade.value }}</td>
            <td>
                <a href="{{ url_for('edit_grade', grade_id=grade.id) }}">Редагувати</a> |
                <a href="{{ url_for('delete_grade', grade_id=grade.id) }}">Видалити</a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
{% else %}
<p class="muted">Поки що немає жодної оцінки.</p>
{% endif %}
{% endblock %}


