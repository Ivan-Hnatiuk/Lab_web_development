{% extends "base.html.j2" %}
{% block content %}
<div class="card">
    <h2>Редагувати оцінку</h2>
    <p class="muted">
        Змініть студента, дисципліну або значення оцінки та збережіть зміни.
    </p>

    {% if error %}
        <p style="color: #b00020; font-weight: 600;">{{ error }}</p>
    {% endif %}

    <form method="post">
        <div style="margin-bottom: 1rem;">
            <label for="student_id">Студент:</label><br>
            <select id="student_id" name="student_id" required>
                {% for student in students %}
                    <option value="{{ student.id }}"
                        {% if student.id == grade.id_student %}selected{% endif %}>
                        {{ student.name }}
                    </option>
                {% endfor %}
            </select>
        </div>

        <div style="margin-bottom: 1rem;">
            <label for="course_id">Дисципліна:</label><br>
            <select id="course_id" name="course_id" required>
                {% for course in courses %}
                    <option value="{{ course.id }}"
                        {% if course.id == grade.id_course %}selected{% endif %}>
                        {{ course.title }}
                    </option>
                {% endfor %}
            </select>
        </div>

        <div style="margin-bottom: 1rem;">
            <label for="value">Оцінка (0–100):</label><br>
            <input type="number" id="value" name="value" min="0" max="100" step="0.1"
                   value="{{ grade.value }}" required>
        </div>

        <button type="submit">Зберегти зміни</button>
        <a href="{{ url_for('edit_grades_list') }}" style="margin-left: 1rem;">Скасувати</a>
    </form>
</div>
{% endblock %}


